#!/bin/bash

# $1 : 回数
# $2 : 戻り値
# $1回目に呼び出された際の出力設定ファイルを作成する。

stubDir=$(dirname $0)

callcountStr=$(echo $1 | awk '{printf("%03d", $0)}')
echo -n "$2" > ${stubDir}/SHELLNAME.expect.output${callcountStr}
